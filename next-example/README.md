Next Example — SpeedInsights

Quick start:

1. cd next-example
2. npm install
3. NEXT_PUBLIC_SPEED_INSIGHTS_API_KEY=<your_key> npm run dev

Open http://localhost:3000

This example wraps the app with `SpeedInsights` in `pages/_app.js`.
